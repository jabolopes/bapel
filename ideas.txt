- Switch to new typechecker.
- Typeclasses for ad-hoc polymorphism, e.g., Number types, Boolean types, operators.
- Entity component system.
- Goal: low-level language for compiler backend; scripting language for games.

- Continuation passing style (CPS).
- Parametric polymorphic functions, e.g., a function that does not depend on the type of the argument
- Ad-hoc polymorphism (e.g., like printU and printS) for user-defined functions.
- LLVM code generation.
- Add array types.
- ECS is the heap.
- STM or atomics for concurrency.
- different types of translation units, e.g., modules, implementations, programs. modules can have an impls section that imports implementation files. implementation files automatically export all of their functions. see module partitions.

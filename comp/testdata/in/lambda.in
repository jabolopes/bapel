module lambda

fn lambda() -> i8 {
  let add: i8 -> i8 = fn (x: i8) { x + 1 [i8] }
  add 2
}

pub fn lambda2() -> i8 {
  let add: i8 -> i8 = fn (x: i8) { x + 1 [i8] }
  add 2
}

pub fn lambda3() -> i8 {
  let add: i32 -> i32 = fn (x: i32) {
    if x > 0 {
      return 0 [i32]
    }
    1 [i32]
  }

  let add2 = fn (x: i32) {
    if x > 0 {
      return 0 [i32]
    }
    1 [i32]
  }

  0
}
